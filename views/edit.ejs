<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Edit</title>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <button  class="ml-280 bg-red-500 p-2 outline-none rounded-md mt-5 ml-3 " onclick="window.location.href ='/logout'">Logout</button>  
        </div>
        
        <h2 class="px-1 mb-5">realign your thoughts.</h2>
        <form action="/update/<%= post._id%>" method="post">
            <textarea placeholder="puke your mind here 🤮." class="block outline-none p-3 resize-none bg-transparent border-2 border-zinc-800 rounded-md w-1/3 " name="content" id="" > <%= post.content%> </textarea>
            <input class="block bg-yellow-500 p-3 outline-none rounded-md mt-5 ml-3 text-black" type="submit" value="Update post">
        </form>
    </div>

</body>
</html>